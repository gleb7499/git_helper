Now follow the instructions below strictly and step by step, without any improvisation!

After completing the project, run a local super-linter analysis using only my own utility:

1) Run the utility from the command line exactly from the root of this project: quick_lint

2) If any program execution error appears in the command line that is not related to linter errors, abort the session. The linter checks will take about 30-35 seconds; wait and do NOT give up waiting until any message appears in the command line (but by default, if nothing has happened within 2 minutes after launching the utility, abort the session so as not to wait forever);

3) If the linter shows any errors, then:
3.1) parse the result;
3.2) go through the project, step by step and carefully fixing the errors the linter found;
3.3) after fixing all errors, go back to step 1;

4) If the linter shows NO errors, then:
- commit
- push
- end the session.
